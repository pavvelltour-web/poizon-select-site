# Публичное имя Telegram-бота без @. Это не секрет.
# Никогда не добавляйте сюда BOT_TOKEN.
VITE_BOT_USERNAME=

# Публичный same-origin путь к CRM. Не URL поставщика и не секрет: браузер
# запрашивает только /api, а Nginx передаёт его FastAPI CRM на сервере.
VITE_CRM_API_BASE_URL=/api
