# Публичное имя Telegram-бота без @. Это не секрет.
# Никогда не добавляйте сюда BOT_TOKEN.
VITE_BOT_USERNAME=
# Public API origin for website checkout. Empty means same-origin `/api`.
VITE_API_BASE_URL=
